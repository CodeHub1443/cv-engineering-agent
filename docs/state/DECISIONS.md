# DECISIONS

> **Append-only, one line per decision.** The ledger of what is settled, so a fresh
> session can absorb the project's commitments in thirty seconds without reading every
> ADR. Any *architectural* decision listed here must have an ADR; if it does not, write
> the ADR before committing.

| # | Date | Decision | Canon | ADR | Status |
|---|---|---|---|---|---|
| D-001 | 2026-08-30 | `docs/PROJECT.md` is frozen canon; all normative statements cite `[P§n]`; changes to intent happen only via ADR | `[P§35]` | — | accepted |
| D-002 | 2026-08-30 | Rolling state split into *state* (`STATUS.md`, rewritten) and *history* (`JOURNAL.md`, append-only) | `[P§25]` | — | accepted |
| D-003 | 2026-08-30 | No implementation code without an issue; no architectural code without an ADR | `[P§34]` | — | accepted |
| D-004 | 2026-08-30 | Design order fixed: registry → gateway → orchestration state → memory → tools → knowledge → skills → stages → execution | `[P§33]`, `[P§34]` | — | accepted |
| D-005 | 2026-08-30 | Capability / skill / tool / agent kept as four distinct typed registries | `[P§23]` | ADR-0001 | **proposed** |
| D-006 | 2026-08-30 | Two independent canons, not one chain: `docs/PROJECT.md` (knowledge) and `CLAUDE.md` (instructions, mirrored by `AGENTS.md`). Raw pre-canon draft moved from repo root to `docs/archive/PROJECT_SOURCE_DRAFT.md`, marked superseded and non-authoritative | `[P§35]` | — | accepted |
| D-007 | 2026-09-01 | Retired `spec/00,01,02,07,09` (fully duplicated `docs/PROJECT.md` or conflicted with ADR-governed `docs/architecture/OVERVIEW.md` / `docs/APPROVALS.md` / `docs/state/EXPERIMENTS.md`); reduced `spec/03,05` to technical elaborations citing the canon; merged unique gated actions from `spec/07` into `docs/APPROVALS.md`; extended `docs/state/EXPERIMENTS.md`'s schema with `spec/09`'s useful fields (`status`, `parent_exp_id`, `approval_ref`, timestamps) | `[P§35]` | — | accepted |
| D-008 | 2026-09-01 | Git workflow: single trunk `main` + short-lived `feature/<owner>/<work>` branches, PR reviewed and merged by the project owner. Two-trunk `main`/`dev-munna` model with a two-stage PM approval gate is retired — `dev-munna` is not a mandatory or permanent branch. `CLAUDE.md`, `AGENTS.md`, `docs/development/GITHUB_FLOW_V1.md`, `docs/development/GITHUB_DEVELOPMENT_MODEL.md` all updated to describe the same model | — | — | accepted |
| D-009 | 2026-09-01 | Capability registry semantics: DECLARED CAPABILITY != EXECUTABLE CAPABILITY. Introduced `status: "planned"` (specified, no executable binding) distinct from `"available"` (specified AND a verified executable binding exists). All 20 capabilities in `spec/capability_registry.json` corrected from `"available"`/`"partial"` to `"planned"` — none has an executable binding. `check_item()` no longer hardcodes `available: True`; reports `executable: False` with a reason for every registry item. `select()`/`is_available` now require `status == "available"` strictly | `[P§23]` | ADR-0001 (§8a) | accepted |
| D-010 | 2026-09-08 | Step 2 foundation: `cv_agent/skills/` adds a `Skill` domain model (independent from `RegistryItem`), a `SkillSource` protocol, `LocalSkillSource` (scans `~/.claude/skills` + `~/.agents/skills`, overridable via `CV_AGENT_SKILL_PATHS`), `SkillInventory`, and `TaskResolver` — deterministic keyword-overlap resolution only, no LLM. Three-state availability model made explicit: DECLARED (in `spec/capability_registry.json`) / DISCOVERED (found on disk by a `SkillSource`) / EXECUTABLE (a verified invocation binding — none exists, so every `Skill.executable` is `False`). New CLI: `skills`, `capabilities`, `resolve "<task>"` | `[P§23]`, `[P§15]` | ADR-0007 | accepted |
| D-011 | 2026-09-15 | Step 4 foundation (Step 3 — skill execution — was requested but never implemented; skipped, not silently assumed done): `cv_agent/requirements/` adds `RequirementsAnalyzer`, deterministic keyword-rule-based known/unknown/assumed field extraction (a field is `"assumed"` only via an explicit caller-supplied assumption — the analyzer never self-promotes), CV task-component hypotheses with stated rationale (`FIELD_DETECTORS`/`TASK_HYPOTHESIS_RULES` as flat, appendable data, not a decision tree), capability links produced by calling the existing `TaskResolver` (not bypassed, not duplicated), and targeted clarification questions. Optional `LLMProvider` (mock only) used solely for a `narrative_summary` field, never as a source of any structured fact. New CLI: `analyze "<request>"`. `CVAgent.analyze_requirements()`. Does not execute anything | `[P§5]`, `[P§6]`, `[P§23]` | ADR-0008 | accepted |
| D-012 | 2026-09-15 | Step 3 (redone properly, not skipped this time): `cv_agent/execution/` adds the skill execution boundary — `SkillExecutionRequest`/`Result`/`Status` (`not_executable\|started\|completed\|failed\|rejected`), `ExecutionBinding` (skill_id -> runtime_id, `approval_policy`, `verified: bool`), a provider-agnostic `ExecutionRuntime` protocol, a deterministic `ExecutionBindingRegistry`, and `SkillExecutor`. Inspected all 84 real installed skills first: every `SKILL.md` is prose for an LLM agent to read and act on with its own tools (28 declare Claude-Code-native `allowed-tools:` frontmatter), not a program with a verifiable invocation contract — so **zero bindings are registered anywhere in this codebase**; building a fake adapter that treated "reading SKILL.md" as execution was explicitly rejected as silent invention. `CVAgent.execute()`/`.can_execute()`/`.execution_bindings`; new CLI `executions`, reporting `Executable: 0/84` against the real environment, honestly | `[P§15]`, `[P§21]`, `[P§22]`, `[P§23]`, `[P§24]`, `[P§34]` | ADR-0009 | accepted |
| D-013 | 2026-09-15 | Orchestration state + human-approval interrupts (identified by a repository alignment review as the actual critical-path blocker, not Research/RAG): `cv_agent/graph/workflow.py` adds a second, separate compiled LangGraph graph using the real dynamic-interrupt API (`langgraph.types.interrupt()` + `Command(resume=...)`, confirmed empirically against the installed langgraph 1.2.11 — no custom sleep/polling) for requirements-clarification (loops back to `analyze_requirements` exactly once after a human answers, never interrupts twice) and approval-gated execution (`approval_gate` node reads a binding's `approval_policy` via `SkillExecutor.get_binding()`, interrupts if `approval_required`, and the resume value — not any other code path — becomes `approval_decision`; the existing `SkillExecutor.execute()` from ADR-0009 still does the actual allow/reject enforcement, so the two layers can't disagree). `AgentState` extended with `requirements_analysis`/`clarification_answers`/`pending_execution`/`approval_decision`/`execution_result`, stored as plain dicts (`dataclasses.asdict()`), not reasoning-layer dataclass instances — LangGraph's checkpoint serializer already warns unregistered classes will be rejected in a future version. Kept as a *second* graph, not inserted into the existing `build_graph()`/`run()` stub — existing tests hard-assert that topology's exact current behavior. Partially resolved `OPEN_QUESTIONS.md` Q1/Q2 (don't block this ADR after all) and Q3 (transport is the interrupt; durable/restart-survivable resume is a checkpointer swap, deferred — `MemorySaver` does not survive process restart, confirmed). `CVAgent.start_workflow()`/`.resume_workflow()`/`.get_workflow_state()`; new CLI `workflow` | `[P§5]`, `[P§10]`, `[P§21]`, `[P§24]`, `[P§29.8]`, `[P§34]` | ADR-0003 | accepted |
| D-014 | 2026-09-16 | First real execution binding (ADR-0009 §8 revisit trigger fired, for exactly one skill): `cv_agent/execution/runtimes/trt_perf_analysis.py` adds `TrtPerfAnalysisRuntime` (an `ExecutionRuntime` implementing skill chosen by inspecting the real installed `trt-perf-analysis` skill directory, not assumed from ADR-0009's own prior mention of it), `build_binding()`, and an explicit, opt-in `register(registry)` — `CVAgent.__init__` still constructs an empty registry; nothing calls `register()` automatically, so `python -m cv_agent executions` against a fresh `CVAgent` still reports `0/84` unless a caller has explicitly opted in. The runtime derives the script's location from the real, discovered `Skill.location` (never a hard-coded path) and runs `scripts/analyze_trt_perf.py` as a real subprocess with this process's own interpreter — verified empirically, not assumed: exit `0` + one JSON object on stdout on success (including a per-backend-failed-validation case, which the script itself treats as a valid outcome, not a crash), exit `2` + a one-line stderr message when no input files exist at all. `approval_policy="allowed"` (read-only, local, deterministic, Python-stdlib-only — no approval gate needed per `CLAUDE.md` §3 rule 10), `verified=True` for this one binding only. No other skill's `Skill.executable` changes. See ADR-0009 §9 | `[P§15]`, `[P§21]`, `[P§22]`, `[P§23]`, `[P§29.9]`, `[P§34]` | ADR-0009 | accepted |
| D-015 | 2026-09-15 | `OPEN_QUESTIONS.md` Q1 resolved by owner decision: V1 uses **one CV project per repository/workspace** — the workspace/repo IS the project boundary, no `project_id`/multi-project/multi-tenant abstraction in V1, no broader multi-repository workspace abstraction. Project Understanding is persistent current state with recoverable, immutable revision history; experiments stay immutable append-only records (unchanged from `EXPERIMENTS.md`); session identity (`AgentState.session_id`) stays distinct from and subordinate to project identity — every session belongs to the one implicit project. ADR-0004 records this and defines the memory subsystem's boundary: `cv_agent/memory/` — `ProjectUnderstandingRevision`, `SessionRecord`, and a `ProjectMemoryStore` `Protocol`, **interface only**, zero implementation, nothing wired into `CVAgent`/the graph/CLI yet. Storage backend deliberately left undecided (`OPEN_QUESTIONS.md` Q8 remains open and is the actual implementation blocker) | `[P§21]`, `[P§25]`, `[P§26]`, `[P§30]`, `[P§33]`, `[P§34]`, `[P§35]` | ADR-0004 | accepted |
| D-016 | 2026-09-15 | `OPEN_QUESTIONS.md` Q15 (new, raised and resolved same day — an ADR-0004 audit found the ADR named a storage backend as open, Q8, but never addressed Git-tracking or data sensitivity) resolved by owner decision: Project Understanding is **potentially sensitive project data**, governed by `docs/APPROVALS.md`'s data/privacy rule; it must be **durable across process restarts** but must **NOT** be automatically stored in Git-tracked repository files — V1 persistent storage is **local/project-scoped, gitignored by default**. Durability does not imply Git tracking. Does not change `EXPERIMENTS.md`'s contract. Does not grant any new external-LLM transmission permission — existing approval/privacy rules still govern that regardless of persistence. `OPEN_QUESTIONS.md` Q8 (storage *technology* — files/SQLite/service) stays open and unresolved, now narrowed to exclude "in-repo/tracked files" as an option for project memory specifically. ADR-0004 updated to record this (§1, §2, §6, §9) — no code, no backend chosen, no `cv_agent/memory/` implementation | `[P§24]`, `[P§25]` | ADR-0004 | accepted |
| D-017 | 2026-09-15 | `OPEN_QUESTIONS.md` Q8 resolved by owner decision, for **project memory only**: **SQLite**, local and project-scoped, living in the project's gitignored persistent-state area (per D-016 — not Git-tracked), must survive process restarts. SQLite is used strictly **behind** the `ProjectMemoryStore` `Protocol` (ADR-0004 §5) — no module outside `cv_agent/memory/` may depend on a SQLite-specific type, so a future backend can replace it without touching callers. No external database/service required for V1. Project Understanding revisions stay immutable; `SessionRecord`s keep their defined mutable-index lifecycle (ADR-0004 §3). `docs/state/EXPERIMENTS.md` is explicitly **not** moved into SQLite and its contract is unchanged — its own backend question is split off as new `OPEN_QUESTIONS.md` Q16, still open, non-blocking for ADR-0004. This resolves Q8 as ADR-0004's last remaining implementation blocker; ADR-0004 is now **ready for implementation** (no code written yet — `cv_agent/memory/` remains unimplemented) | `[P§21]`, `[P§25]`, `[P§29.5]`, `[P§34]` | ADR-0004 | accepted |
| D-018 | 2026-09-15 | ADR-0004 implemented: `cv_agent/memory/` — `models.py` (`ProjectUnderstandingRevision`, `SessionRecord`, backend-neutral), `store.py` (`ProjectMemoryStore` `Protocol`, `ProjectMemoryError`, `default_db_path()` — zero `sqlite3` references), `sqlite_store.py` (`SqliteProjectMemoryStore` — the only module in the codebase importing `sqlite3`, verified by a structural test). Two tables (`understanding_revisions` append-only with autoincrement ordering; `sessions` upserted by `session_id`), idempotent schema init, every write transactional, every failure raised as `ProjectMemoryError`, never swallowed. `default_db_path()` resolves from `Path.cwd()` (or an explicit `workspace_root`), deliberately not from `__file__`'s install location, so an installed `cv-agent` does not silently share one database across unrelated projects — the concrete enforcement of the Q1 "workspace IS the project" decision at the filesystem level. `.gitignore` gained `/.cv_agent/`. 28 new tests (`tests/test_memory.py`): init, empty-store, immutable/ordered revision history, duplicate-id rejection, session upsert, persistence across close/reopen (including 3 restart cycles), closed-connection error behavior, and the `sqlite3`-confinement structural test. Deliberately **not** wired into `CVAgent`/the workflow graph/CLI (ADR-0004 §6/§9's own stated scope) — that remains a separate, later PR. Full suite 195 → 223 passing, zero regressions. Branch `feature/claude/project-memory`, not committed to `main` | `[P§21]`, `[P§25]`, `[P§30]`, `[P§34]` | ADR-0004 | accepted |
| D-019 | 2026-09-15 | Workspace-root resolution contract clarified by owner decision, following a post-implementation audit that found `default_db_path()`'s `Path.cwd()` fallback had no actual guarantee behind it (no CLI flag, no `CVAgent` parameter, no other mechanism anywhere ties process execution to a directory; the installed `cv-agent` console script is invocable from anywhere — silent failure modes: wrong-directory invocation, subdirectory fragmentation, or two unrelated projects sharing one `.cv_agent/` by coincidence). Resolved: **the calling application is responsible for resolving `workspace_root`**, never `ProjectMemoryStore`/`default_db_path()` itself; `Path.cwd()` remains only as a documented convenience default for direct/standalone use, explicitly not the application contract; the not-yet-built `CVAgent`/CLI integration (ADR-0004 §9) must resolve and pass `workspace_root` explicitly when it wires a store in; automatic repository-root discovery (`.git`-walking) is explicitly out of scope for V1, named as a revisit trigger instead. Documentation-only — ADR-0004 §1 (new item 13), §2, §5, §8, §9 updated; no code changed, `cv_agent/memory/` behavior is unchanged (it already accepted an explicit `workspace_root` override) | `[P§21]`, `[P§34]` | ADR-0004 | accepted |
| D-020 | 2026-09-15 | Project Memory (ADR-0004) wired into the runtime, minimally: `AgentConfig.workspace_root: Path \| None = None` (new field, not TOML-sourced — a per-invocation fact, not a static setting); `cv_agent.memory.store.open_store()` (new factory, mirrors `cv_agent.llm.registry.get_provider()` — callers ask for "the store," never import `SqliteProjectMemoryStore` directly, keeping SQLite confined even at this package's own public entry point); `CVAgent.memory` (public, **lazily** constructed on first access — `health_check()`/`resolve()`/`analyze_requirements()`/`execute()` never touch disk for this, only `start_workflow()`/`resume_workflow()` do); a `SessionRecord` written before the graph runs (status "running") and updated after (status/`ended_at` from the resulting `AgentState`, restart-safe — re-running `start_workflow()` for an existing `session_id` preserves its original `started_at`). Revision-trigger policy (ADR-0004 §9, previously deferred to this PR): append a new `ProjectUnderstandingRevision` iff a run's `requirements_analysis` differs, by JSON-normalized structural equality **excluding `narrative_summary`/`llm_provider`**, from `get_current_understanding()` — not semantic diffing, a fixed comparison. Two things were found and fixed only by writing the integration tests, not anticipated in ADR-0004: (1) `AgentState["requirements_analysis"]`'s container types are not stable — tuples on a fresh `.invoke()`, lists once round-tripped through a LangGraph checkpoint — a bare `==` against this store's own JSON-stored value reported "different" for identical data; (2) even the mock LLM provider re-words `narrative_summary` on every call (a response counter), so comparing prose meant the dedup *never* fired, including for a plain restart of the same task — excluded via a fixed field list grounded in ADR-0008's own "prose, never a source of fact" classification. Also fixed: `tests/test_workflow.py`'s `TestCVAgentWorkflowWiring` and `tests/test_cli.py`'s CLI-subprocess `_run()` helper had been constructing `CVAgent()`/running the CLI with no explicit `workspace_root`/`cwd` — since default resolution now falls back to `Path.cwd()`, both were silently creating a real `.cv_agent/memory.sqlite` inside this repository's own working directory on every test run, undetected until this integration made memory-writes observable; both pinned to their existing `tmp_path` fixture. `cv_agent/graph/workflow.py` itself is untouched — no memory call inside any graph node; persistence happens in `CVAgent` wrapping the graph invocation, keeping ADR-0003's checkpoint/interrupt mechanics and this ADR's durable store two separate concerns, per ADR-0004 §2. 24 new tests (21 in new `tests/test_memory_integration.py` + 3 `open_store()` tests in `tests/test_memory.py`). Full suite 223 → 247 passing, zero regressions. Known limitation, not fixed here (`ProjectMemoryStore`'s interface has no cross-write transaction primitive): the session-record write and the revision write inside one sync call are two independent atomic operations, not one — a failure between them is a narrow, acknowledged gap, not silently ignored. Branch `feature/claude/project-memory`, not committed to `main` | `[P§21]`, `[P§25]`, `[P§30]`, `[P§34]` | ADR-0004 | accepted |
| D-021 | 2026-09-16 | Closed the requirements -> resolution -> execution feedback loop at the application layer (an architecture audit found this, not another execution binding/RAG/research/real LLM/persistent checkpointing, was the actual highest-value gap: the one real binding from D-014 was genuinely invocable only from Python/tests, never from the CLI, and `Skill.executable`/`SkillMatch.executable` were hardcoded `False` everywhere, disconnected from the real `ExecutionBindingRegistry`). Two amendments, no new ADR: **(1) ADR-0007 §9** — `SkillInventory` gains an optional `is_executable: Callable[[str], bool] \| None` predicate (never a concrete `cv_agent.execution` import — `cv_agent.skills` stays execution-agnostic per this ADR's own §2 boundary); `CVAgent.__init__` wires `self._executor.can_execute` in; `TaskResolver` now copies `skill.executable` instead of hardcoding `False`. No predicate given (every pre-existing direct `SkillInventory(...)` construction) reproduces the exact prior behavior. **(2) ADR-0009 §10** — new `python -m cv_agent execute <skill_id>` CLI command, real `CVAgent -> SkillExecutor -> ExecutionBindingRegistry -> ExecutionRuntime` path, never bypassed/duplicated; only `trt-perf-analysis` is accepted (a single constant check, not a dispatch table); this command is the one explicit, controlled caller that registers that binding into its own short-lived `CVAgent`; `--path`/`--input KEY=VALUE`/`--model-name` build the real `SkillExecutionRequest`; `--approve` or a live "y"/"yes" prompt answer is required for a hypothetical future `approval_required` binding — never auto-approved (`_confirm_approval()`, unit-tested against a fake binding since `trt-perf-analysis`'s own policy is `"allowed"`). `skills`/`resolve`/`capabilities`/`executions` are untouched and still report `executable=False`/`0/84` by default — no auto-registration anywhere outside this one command. 32 new tests (10 in `tests/test_skills.py`/`tests/test_agent.py` for the resolution wiring, 17 in new `tests/test_cli_execute.py` for the CLI, plus a genuine, unfaked end-to-end CLI smoke test against the real installed skill). Full suite 278 → 305 passing, zero regressions. Also fixed `docs/state/STATUS.md`'s stale "PR #27 still open" statement (merged as `2cf3709`). Branch `feature/claude/execution-feedback-loop`, not committed to `main` | `[P§15]`, `[P§19]`, `[P§21]`, `[P§22]`, `[P§23]`, `[P§24]`, `[P§34]` | ADR-0007, ADR-0009 | accepted |
| D-022 | 2026-09-16 | Exposed matched skill information, including live executable status, from `RequirementsAnalysis` (an architecture audit of merged `main` @ `7c284d2` identified this as the single highest-priority next step: `RequirementsAnalyzer._link_capabilities()` was already calling `TaskResolver.resolve()` once per candidate task component and reading `result.matched_capabilities`, but silently discarded `result.matched_skills` — including the truthful `executable` flag D-021 had just wired up — on the same line, so a user running `analyze` had no way to learn which matched capability actually had a right-now-executable skill behind it). **ADR-0008 §9 amendment:** `RequirementsAnalysis` gains a new top-level `skill_links: tuple[SkillLink, ...]` field (`SkillLink`: `task_component`, `skill_id`, `declared`, `matched_terms`, `executable`) — deliberately NOT nested inside `CapabilityLink`, since a resolver's `SkillMatch` is not attributed to one specific capability within a single `resolve()` call (a skill can be matched purely by keyword overlap, independent of any capability's declared `relevant_skills`, or declared by more than one matched capability at once); `task_component` is the same join key `CapabilityLink` already uses. `_link_capabilities()` now returns `(capability_links, skill_links)` from the SAME `resolve()` call already in scope — no second resolution pass (verified by an explicit call-count test). `executable` is copied verbatim from `SkillMatch.executable`, itself copied from the calling `CVAgent`'s live execution registry at analysis time (ADR-0007 §9) — never a permanent property of the skill, never independently computed. No `selected_skill` field, no ranking, no confidence-of-selection — `RequirementsAnalyzer` still does not decide which skill should execute (ADR-0008 §2's boundary, unchanged, now simply made visible rather than moved). `python -m cv_agent analyze` gained a "Matched skills" section, formatted consistently with `resolve`'s own output; `analyze` still constructs a fresh, unregistered `CVAgent` and never registers a binding itself, so a bare CLI invocation still always reports `executable=False`. 15 new tests (9 in `tests/test_requirements.py::TestSkillLinks` — empty/one-skill/multiple-skills/multiple-capabilities/task-component-scoping/no-second-call using a duck-typed fake resolver for precise control, plus executable True/False/unverified-binding against a real fixture skill named `trt-perf-analysis`; 4 in `tests/test_agent.py` — `CVAgent.analyze_requirements()` end-to-end including one genuine, unfaked test against the real installed `trt-perf-analysis` skill and its real shipped binding; 2 in `tests/test_cli.py`). Full suite 305 → 320 passing, zero regressions. Also fixed, per this task's explicit instruction: `docs/roadmap/ROADMAP.md` Phase 3's stale "zero bindings are registered... no `ExecutionRuntime` implementation exists for any real skill yet" and Phase 4's stale "no memory subsystem exists" status statements, both predating D-020/D-021. Branch `feature/claude/requirements-skill-links`, not committed to `main` | `[P§5]`, `[P§6]`, `[P§23]`, `[P§34]` | ADR-0008 | accepted |
| D-023 | 2026-09-17 | Added an explicit, pre-supplied execution-input channel closing the gap ADR-0010 §10 (merged, PR #31) had already named honestly: `plan_execution` always called `plan_execution()` with `available_inputs={}` because nothing in `AgentState` represented "a caller already knows a required execution input's value before this run starts." **ADR-0010 §12 amendment:** new `AgentState.execution_inputs: dict[str, Any]`, keyed by `InputField.name` (ADR-0009 §11) — a deliberately distinct namespace from `clarification_answers` (keyed by `RequirementField.name`); the two are never merged or cross-read, preserving `[P§35]`'s no-silent-inference rule. New `CVAgent.start_workflow(execution_inputs=...)` keyword-only parameter, defaulting to `{}` (unchanged behavior for every existing caller). `_node_plan_execution` reads `state.get("execution_inputs") or {}` in place of the hardcoded `{}` — the only line changed in `cv_agent/graph/workflow.py`'s node body; `cv_agent.graph.planning.plan_execution()` itself is untouched. Caller-supplied `pending_execution` precedence is unaffected — when present, `execution_inputs` is simply unused for that run, never merged. V1 is pre-supply only, by explicit owner scope decision: no CLI flag (API parameter only), no same-session retry/interrupt when the value is discovered only after `missing_required_inputs` (Q17, reworded to describe this narrower remaining gap, not struck through), no cross-binding field-name collision guard (documented as a future consideration for when a second individually-verified binding exists, ADR-0009 §8's per-skill trigger). 5 new tests: 3 in `tests/test_workflow.py` (`TestPlanExecutionIntegration::test_execution_inputs_satisfy_missing_required_input_and_produce_a_plan`/`test_execution_inputs_survive_the_clarification_loop`, `TestManuallySuppliedPendingExecutionPrecedence::test_execution_inputs_are_ignored_when_pending_execution_is_supplied`), 2 in new `tests/test_memory_integration.py::TestExecutionInputsChannel` (real `CVAgent.start_workflow()` API + real skill discovery, both the supplied-value-produces-a-plan case and the omitted-value-preserves-prior-behavior case). Full suite 357 → 362 passing, zero regressions. Branch `feature/claude/execution-input-channel`, not committed to `main` | `[P§19]`, `[P§21]`, `[P§34]`, `[P§35]` | ADR-0010 | accepted |

| D-024 | 2026-09-17 | Same-session recovery from `plan_execution` returning `missing_required_inputs` (Q17's remaining sub-case, ADR-0010 §12 had already closed the "known in advance" half): a new `provide_execution_inputs` interrupt node — architecturally consistent with the existing `clarify` interrupt (ADR-0003), a distinct namespace from `clarification_answers` — inserted between `plan_execution` and `approval_gate`, bounded to exactly one interrupt/resume round per run. `PlanningResult` (`cv_agent/graph/planning.py`) gains `selected_skill_id`/`selected_binding_id`/`selected_input_schema`, populated for both `"planned"` and `"missing_required_inputs"` — a checkpointed identity+contract snapshot, never a live registry re-lookup at comparison time (replay-unsafe otherwise, confirmed empirically: LangGraph re-runs a node's pre-interrupt code on every resume). `AgentState.execution_input_recovery` (new) records a deterministic classification (`supplied`/`incomplete`/`invalid`/`cancelled`/`binding_mismatch`) and a single `terminal` boolean; `pending_execution` may be set from a recovery round only when the retry's freshly-selected identity AND full `input_schema` structurally match what was recorded before the human was asked — ID equality alone is explicitly not trusted, since a same-`binding_id` binding's contract could change shape underneath the pause. Any non-`supplied`-confirmed outcome bypasses `approval_gate` entirely, routing straight to `END`, so a failed recovery can never be read as "approval not required" for a plan that was never produced. Caller-supplied `pending_execution` precedence and all first-pass routing (`planned`/`no_executable_candidate`/`ambiguous_candidates`) are unchanged. Two real bugs found only via testing, not anticipated in design: `status` never reached `"done"` on the new bypass-to-`END` path (fixed — `_node_plan_execution` now sets it directly when finalizing a terminal recovery outcome, since `approval_gate` — which previously did this — is skipped); `Command(resume={})` (a literal empty dict) is not reliably delivered by the installed LangGraph (confirmed empirically — the graph silently re-pauses instead of resuming), contradicting this codebase's own pre-existing `clarify`-interrupt test comment that claimed an empty mapping was the correct "no answer" signal — not fixed for `clarify` (out of scope), documented instead as a `CVAgent.resume_workflow()` docstring caveat. `trt_perf_analysis.build_binding()`'s real input contract, verified by reading `_build_argv()` directly, is a genuine `path`/`data` XOR that `InputField.required: bool`'s flat model cannot express truthfully — deliberately left unpopulated; every test uses a synthetic fixture binding instead; spun off as new, separate `OPEN_QUESTIONS.md` Q20 (not resolved here). 21 new/updated tests (5 in `tests/test_execution_planning_contract.py::TestPlanningResultSelectedIdentity`, 14 new + 2 updated in `tests/test_workflow.py`). Full suite 362 → 379 passing, zero regressions. Branch `feature/claude/q17-input-recovery` | `[P§19]`, `[P§21]`, `[P§24]`, `[P§34]`, `[P§35]` | ADR-0010 | accepted |

| D-025 | 2026-09-18 | `python -m cv_agent workflow` gained real, caller-supplied input handling across all three interrupt kinds (`clarify`, `provide_execution_inputs` since D-024, `approval_gate`), replacing auto-fabricated placeholder answers (ROADMAP Phase 4's own already-named gap). CLI design: `--answer`/`--input` KEY=VALUE flags (repeatable; `--input` reuses `execute`'s existing convention and doubles as both the ADR-0010 §12 pre-supply channel and a recovery-round answer source), `--approve`/`--reject` (mutually exclusive), falling back to a live stdin prompt for anything a flag doesn't cover — never a fabricated value, same "ask once, stop, never silently approve" posture `_confirm_approval` (D-021) already established. Pure CLI-layer glue (`cv_agent/__main__.py` only) over the already-accepted `CVAgent` API — no ADR, no graph/state change. `_cmd_workflow` deliberately still never registers any execution binding (same honesty default as `analyze`/`resolve`/`skills`), so `approval_gate`/`provide_execution_inputs` remain real and generic but structurally unreachable through this CLI against any real installed skill today — verified instead via a fixture-graph-backed test double and one manual run against a real `CVAgent` with a synthetic binding, both honestly labeled as such (same Q20 posture). Found, but explicitly did not fix (out of this decision's scope), a genuine pre-existing bug: `cv_agent/graph/workflow.py`'s `_route_after_analysis` routes back to `clarify` based on `bool(state.get("clarification_answers"))` — truthiness, not "already attempted" — so a human declining every clarification question causes an unbounded repeat interrupt (reproduced directly, not hypothesized). Mitigated only CLI-side with a `_MAX_INTERRUPT_ROUNDS` safety cap (`WorkflowStuckError`) so the command aborts cleanly instead of hanging; the real fix is spun off as new `OPEN_QUESTIONS.md` Q21. 22 net new tests (21 in new `tests/test_cli_workflow.py`, 1 new + 2 rewritten in `tests/test_cli.py`). Full suite 381 → 403 passing, zero regressions; `ruff`/`mypy` clean on all touched/new files (confirmed pre-existing repo-wide findings elsewhere predate this branch via `git stash` diff against `main`). Also corrected a stale, self-contradictory `docs/state/STATUS.md` (kept in its own PR, #35, per explicit instruction, not mixed into this one). Branch `feature/claude/workflow-cli-io`, issue #34, PR pending | `[P§21]`, `[P§24]`, `[P§34]` | none (not architectural) | accepted |

| D-026 | 2026-09-19 | Q21 fixed: `AgentState` gains `clarification_attempted: bool` (new, additive), set unconditionally by `_node_clarify` on every resume — mirrors `execution_input_recovery["attempted"]`'s existing pattern (ADR-0010 §13) rather than inventing a new one. `_route_after_analysis` (`cv_agent/graph/workflow.py`) now routes on this flag, never on `clarification_answers`' truthiness — fixes the confirmed infinite loop when every clarification question is declined. `CVAgent.start_workflow()` initializes the flag to `False`. Separately, confirmed (empirically, 5/5 trials) that `Command(resume={})` never reaches `_node_clarify`'s body at all — the same LangGraph `Command(resume=...)` characteristic ADR-0010 §13 already documented for `provide_execution_inputs`, now confirmed for `clarify` too; fixed by having the CLI (`_resume_value_for_interrupt`'s `"clarification"` branch) resume with `""`, never `{}`, when every question is declined — both fixes were required together, neither alone was sufficient (verified: `""` alone still loops via the routing bug; the routing fix alone doesn't help if the resume value itself never gets delivered). No interrupt payload shape or resume-value contract changed — `AgentState` gains one additive, optional field. ADR-0003 §3's own wrong claim about the loop bound corrected; new `## 9. Status —` section appended (mirrors ADR-0010's established pattern of appended status sections over a new ADR file). Also found and fixed a real test-coverage gap predating this work: `tests/test_workflow.py::test_empty_resume_value_produces_no_fabricated_answers` resumed with `{}` and only asserted the answers dict's value — which passed even under total non-delivery, since that's the field's own `start_workflow()` default; never asserted `"__interrupt__" not in resumed`. Corrected to resume with `""` and check the interrupt actually clears; added a dedicated regression test pinning the `{}`-never-delivered characteristic separately. `_MAX_INTERRUPT_ROUNDS` (D-025) kept as defense-in-depth, docstring updated to no longer describe an active bug. 4 new/updated tests in `tests/test_workflow.py` (2 new, 2 corrected), 1 rewritten in `tests/test_cli.py`, 1 rewritten in `tests/test_cli_workflow.py`. Full suite 403 → 405 passing, zero regressions; `ruff`/`mypy` clean on all touched files (3 pre-existing findings outside this diff's own lines confirmed unchanged from `main`). Manually verified against the real, unfaked CLI: decline-everything now exits 0 with `Final status: done`. Branch `fix/claude/q21-clarify-attempted-flag`, issue #37, PR pending | `[P§21]`, `[P§34]` | ADR-0003 §9 | accepted |

| D-027 | 2026-09-18 | Q18/Q20 (both explicitly "owner decision, nothing to build until answered" per `STATUS.md`) resolved by owner decision, asked directly: Q18 -> a clarification-style interrupt for ambiguous-candidate disambiguation (future work, not implemented this PR — a separate issue); Q20 -> a oneOf/XOR field-group construct on the execution-binding schema. Implemented Q20 only, to keep this PR focused on one gap: new `RequiredFieldGroup` (`cv_agent.execution.binding`, ADR-0009 §12) — `kind: Literal["exactly_one"]`, `field_names: tuple[str, ...]`, presence-only (satisfied once any one member has a value; does not reject "more than one supplied," which stays `TrtPerfAnalysisRuntime._build_argv()`'s own job). `ExecutionBinding` gains `input_field_groups`, validated at construction (every member must be a declared, `required=False` `InputField` — an individually-required field cannot also be a group member). `plan_execution()` (ADR-0010 §14) and the `provide_execution_inputs` recovery interrupt are both made group-aware — recovery fulfillment is "any one group member accepted," not "every requested name," which would have misrepresented a genuine XOR and made real recovery practically unusable. The retry-time identity/schema guard (ADR-0010 §13) now also compares field groups, not only individual fields. `trt_perf_analysis.build_binding()` finally populates its real `path`/`data`/`model_name` contract plus one `exactly_one` group — the gap ADR-0009 §11 and ADR-0010 §13.8 both explicitly left open, blocking a genuine, unfaked end-to-end test of the recovery flow against the real binding (every prior recovery test used a synthetic fixture instead). Found and fixed two things only by writing tests, not anticipated in design: (1) `RequiredFieldGroup.field_names` (a tuple field) is subject to the same tuple-vs-list checkpoint-round-trip instability ADR-0004 already documents elsewhere — fixed by building both sides of the group comparison manually with `field_names` forced through `list(...)`; (2) `still_missing`'s original `requested` minus `accepted` computation didn't know about groups, so supplying only one XOR member still listed the other as "still missing" — fixed by excluding a satisfied group's other members. 26 new/updated tests, including 2 genuine `@requires_real_skill` end-to-end tests against the real installed `trt-perf-analysis` binding (skipped, not faked, where unavailable). Full suite 405 → 429 passing, zero regressions; `ruff`/`mypy` clean except the same already-tolerated `AgentState` "no key `__interrupt__`" TypedDict gap `tests/test_workflow.py` already carried pre-existing. Branch `feature/claude/q20-input-field-groups`, issue #39, PR pending | `[P§19]`, `[P§21]`, `[P§34]`, `[P§35]` | ADR-0009 §12, ADR-0010 §14 | accepted |

| D-028 | 2026-09-18 | Independent review of PR #40 (D-027) found the Q20 implementation satisfied "declarative field-group construct" but under-enforced the decision's own "EXACTLY ONE alternative, not merely at least one" wording — `plan_execution()`/the recovery interrupt only ever checked "at least one member present," deferring "reject more than one" entirely to `TrtPerfAnalysisRuntime._build_argv()`. Corrected, same PR/branch, before merge: new `PlanningStatus` value `"conflicting_inputs"` (ADR-0010 §15) — two or more members of an `"exactly_one"` `RequiredFieldGroup` present in `available_inputs` is now caught and reported (`PlanningResult.conflicting_inputs`, the exact offending names) *before* any plan, approval interrupt, or execution, checked ahead of `"missing_required_inputs"` when both would otherwise apply to the same candidate. `_classify_execution_input_resume()` gains a matching `"conflicting"` outcome for the `provide_execution_inputs` recovery path; a review-identified edge case — a resume payload naming an unrequested field that conflicts with an already-known value from an earlier round, which that function's own per-round `field_groups` reconstruction cannot see — is still caught by `_node_plan_execution`'s retry (a new explicit branch checked ahead of the pre-existing generic "not planned" invariant guard), verified by a dedicated regression test, not merely reasoned about. `ExecutionBinding.__post_init__` (ADR-0009 §13) also gains group-membership-overlap validation — a field may belong to at most one `RequiredFieldGroup` — closing a second review-identified gap the recovery node's group-reconstruction logic implicitly relied on. No interrupt routing change: `"conflicting_inputs"` reuses the existing `approval_gate` no-op fallthrough (like `"ambiguous_candidates"`), never a new interrupt kind. Also fixed, per explicit review findings: `docs/state/STATUS.md` was 4 lines over its own documented 60-line hard cap, trimmed to fit; ADR-0010 §14.7's per-file test-count breakdown did not reconcile against the actual diff (aggregate total of 25 was correct, per-file attribution was not) — recounted directly from `git diff` and corrected. 9 new/1 renamed tests (net 8) across `tests/test_execution_planning_contract.py`, `tests/test_workflow.py`, `tests/test_execution.py`, `tests/test_execution_trt_perf_analysis.py`, including 2 more genuine `@requires_real_skill` tests proving the real binding rejects a "both supplied" conflict before ever invoking the real subprocess (`execution_result is None`). Full suite 429 → 437 passing, zero regressions; `ruff`/`mypy` clean except the same pre-existing findings already documented in D-027, confirmed unchanged against `main`. Q18 untouched, per explicit instruction. Branch `feature/claude/q20-input-field-groups` (same PR #40, not merged), issue #39 | `[P§19]`, `[P§21]`, `[P§34]`, `[P§35]` | ADR-0009 §13, ADR-0010 §15 | accepted |

| D-029 | 2026-09-20 | Q18 implemented (decided by owner 2026-09-18, D-027: a clarification-style interrupt, explicitly not a CLI `--skill` override): a fourth interrupt kind, `choose_candidate`, fires when `plan_execution()` reports `"ambiguous_candidates"`. `plan_execution()` gains an optional `selected_skill_id` (consulted only when >1 candidate; a valid one resolves ambiguity through the *identical* downstream code path as a sole candidate, an unrecognized one is ignored — never a default) and `PlanningResult.candidate_descriptions` (parallel to `candidate_skill_ids`, sourced from `ExecutionBinding.description` so `plan_execution()` keeps its two-dependency boundary — no `SkillInventory`). The interrupt presents every candidate's skill_id + description; the resume value is a bare skill_id string classified deterministically (`"cancelled"` for anything not a non-blank string, `"invalid"` for a string not *exactly* in the offered set — no case folding, no prefix/closest match — else `"selected"`), validated against the checkpointed `planning_result` snapshot only (never a live registry read pre-`interrupt()`, same replay-safety rule as ADR-0010 §13). A validated choice persists to new `AgentState.candidate_choice`, the record to new `candidate_selection`, and control routes back to `plan_execution` for a fresh authoritative retry. One shot, like every other interrupt kind: an invalid/cancelled/no-longer-valid choice is `terminal=True`, bypasses `approval_gate`, sets `status="done"` — never a second prompt, never a default candidate. One real hole found while designing the retry, before any test existed: if the chosen skill's binding is deregistered mid-pause only the *other* candidate remains, `plan_execution()` then sees a single candidate and would plan it — silently running a skill the human never chose. The retry therefore requires `result.selected_skill_id == chosen_skill_id` (an identity check, not merely "still ambiguous?"), else terminal `"candidate_mismatch"`; the pinning test was mutation-checked (weakening the check to "still ambiguous?" makes it fail). Also changed: `_node_plan_execution`'s two recovery-finalization blocks are now gated on `attempted is True AND terminal is None`, since a run can now revisit the node several times (ambiguous → choose → retry → missing input → provide inputs → retry) and each round must be finalized exactly once; behavior-preserving for every pre-existing test. `_route_after_planning` checks candidate disambiguation *first* (no candidate's input completeness can be evaluated until exactly one is selected). Unchanged by design: caller-supplied `pending_execution` (ADR-0010 §10 — still bypasses planning entirely), `python -m cv_agent execute` (separate path), the one-candidate case (never interrupts), `approval_gate` policy (choosing ≠ approving; an `approval_required` chosen skill still reaches approval). CLI: `python -m cv_agent workflow` prompts with every candidate and reports `Candidate selection: outcome=…`; no new flag; `--approve` is never read as a choice. `start_workflow()` gained no parameter (only initializes the two new state keys to `None`). 31 new tests (7 planning-contract, 16 graph-level incl. 7 parametrized invalid/cancelled answers, 2 through the real `CVAgent` API, 6 CLI); full suite 437 → 468, zero regressions; `ruff`/`mypy` on touched files show exactly the findings `main` already has, compared against a disposable `main` worktree (one transient mypy regression from a test-helper aliasing trick was caught and fixed before commit). Branch `feature/claude/q18-candidate-disambiguation`, issue #41, PR pending | `[P§19]`, `[P§21]`, `[P§24]`, `[P§34]`, `[P§35]` | ADR-0010 §16 | accepted |

| D-030 | 2026-09-20 | Independent audit of PR #42 (D-029) found four defects, each reproduced empirically before being fixed, all on the same branch before merge. **D1 (medium):** the human's choice was pinned by `skill_id` only — re-registering the chosen skill under a new `binding_id`/runtime/description during the pause was accepted and the *replacement* runtime ran under the human's earlier choice (`provide_execution_inputs` already pinned binding identity; the direct path did not). Fixed: `PlanningResult.candidate_binding_ids` (parallel to `candidate_skill_ids`) snapshots each offered candidate's `binding_id` at plan time; the choose node records the chosen candidate's `expected_binding_id`/`expected_description` from that checkpointed snapshot (still no live registry read before `interrupt()`); the retry accepts only if the fresh call resolves to the same skill AND `binding_id` AND the current registry description still equals what was shown, else terminal `candidate_mismatch` with `mismatch_detail` `skill_not_resolved`/`binding_changed`/`description_changed` (the live description comparison is correct on the retry — the replay-safety rule forbids live reads only *before* `interrupt()`). Rebinding only the unchosen candidate, or re-registering an identical binding, is correctly not a mismatch. **D2 (documentation):** the classifier docstring and ADR §16.2 listed `None`/`{}` as ordinary "cancelled" answers; they never reach the classifier — the installed LangGraph does not deliver `Command(resume={})` (silent re-pause of the same interrupt, a later real answer still works) and `resume=None` raises an `UnboundLocalError` inside LangGraph (thread left cleanly paused). Corrected in the docstring, ADR §16.2/§16.6 and the `CVAgent.resume_workflow()` caveat (now covering `choose_candidate`); pinned by tests; deliberately NOT worked around (no contract-consistent fix in the supported API). Consequence stated plainly: "one shot, never a second prompt" holds for every *delivered* answer. **D3 (decision, per owner):** on a terminal recovery failure `planning_result["plan"]` is forced to `None` (status and `candidate_*`/`selected_*` kept as diagnostics) — previously a terminal `candidate_mismatch` could leave a full plan for the *other* candidate, i.e. a skill the human explicitly did not choose. Applied to `execution_input_recovery`'s terminal outcomes as well (a terminal `binding_mismatch` planned a binding the human was never asked about — identical rationale, and an invariant that depended on which recovery kind failed would be half-reliable); no pre-existing test asserted a stale plan surviving. Successful recoveries keep their plan. **D4 (hardening):** `_node_choose_candidate` now fails closed — the three offer lists must be non-empty and equal in length, checked before `interrupt()` from checkpointed state — else a terminal `"malformed_offer"` record, `status="done"`, no prompt (previously `zip()` would silently truncate the offered list while validation still accepted the hidden IDs); unreachable today, tested at node level (4 shapes) and end to end via a patched planner. Coverage the audit listed as missing was added: chosen skill removed during the *second* pause of a chained recovery, each recovery record finalized exactly once (step-log count across three `plan_execution` visits), both candidates removed, three-candidate variants (non-chosen removed / chosen removed with two remaining), an unverified-binding swap, checkpoint list normalization. D1 (binding and description comparisons separately), D3 and D4 were mutation-checked: removing each fix fails its tests. 25 new tests (23 in `TestChooseCandidateAuditFixes`, 2 planning-contract); full suite 468 → 493, zero regressions; `ruff`/`mypy` on touched files identical to `main` (a transient regression from a `**kwargs` test-helper signature was caught by that comparison and fixed before commit). Repo has no CI (`.github/workflows` absent), so none of this is CI-verified. Branch `feature/claude/q18-candidate-disambiguation`, PR #42, not merged | `[P§19]`, `[P§21]`, `[P§24]`, `[P§34]`, `[P§35]` | ADR-0010 §16.3/§16.6/§16.7 | accepted |

| D-031 | 2026-09-21 | Owner decisions closing out PR #42 (D-029/D-030) and scoping its follow-ups. (1) **Strict description pinning is kept:** a `choose_candidate` choice is pinned to the binding *shown* — `binding_id` **and** description — so a description reworded after the human read it terminates the run (`description_changed`); a looser `binding_id`-only policy was offered and declined. (2) **D3 plan clearing is approved for `execution_input_recovery`'s terminal `binding_mismatch`**, not only the new interrupt — `planning_result["plan"]` is `None` after any terminal recovery failure. (3) **Fixture-graph and real-`CVAgent` coverage is accepted for #42**; real CLI reachability is tracked separately (#44, Q23). PR #42 merged as `3230361`. (4) **The approval-pause binding-integrity gap is pre-existing and deliberately NOT fixed in #42 or in this docs PR** — an independent audit found, and re-verification reproduced on both `main` (`3230361`) and pre-Q18 `752bc1c`, two defects: a replacement binding/runtime registered during the approval pause executes under approval granted for the original, and a replacement whose policy is `allowed` overrides a human *rejection* (`approval_decision="not_required"`, `completed`), including when it keeps the same `binding_id` and only flips the policy. Tracked as #43 / Q22 — architectural (changes `pending_execution`'s shape), so an ADR amendment (ADR-0003/0009) must precede implementation. Q3 and Q19 remain tracked in `OPEN_QUESTIONS.md`; no duplicate issues created. Documentation only — no source or test change | `[P§24]`, `[P§29.8]`, `[P§34]`, `[P§35]` | ADR-0010 §16; #43 pending ADR | accepted |
| D-032 | 2026-09-21 | Approval integrity (issue #43 / Q22), owner-approved contract implemented. **Decisions:** ADR-0003 owns the approval/execution contract (new §10), ADR-0009 §14 the executor interface, ADR-0010 §17 description pinning; no ADR-0011. `pending_execution` gains `execution_pin`, captured once in `_node_plan_execution`: a canonical whole-`ExecutionBinding` snapshot (explicit field access, declared order, canonical-JSON equality) plus a process-local runtime registration generation (a replaced runtime object, including A→B→A, is detected; same-object re-registration is not a change). Three pin states — missing key, explicit `None` (no binding at capture), present (well-formed/malformed) — with one authoritative outcome table (ADR-0003 §10.5). The gate decides from the pin only (no live registry read before `interrupt()`); `_node_execute` checks a recorded rejection FIRST (no pin validation, no registry read, no executor call), so a rejection cannot be overridden by a replacement binding, policy or runtime; the executor compares the pin against its own single registry read and invokes that same runtime instance; an approval-required execution without a pin is refused even with `approved=True` (rule E1, incl. the direct CLI, which now pins before its prompt). Mismatch is terminal, fail-closed, no re-approval; `ExecutionError` gains category `binding_mismatch` (no new `AgentState` field); the plan is cleared and the recorded decision is never rewritten. Strict description pinning extends through `provide_execution_inputs` (`PlanningResult.selected_description`, `description_changed`, fail closed on an absent snapshot). **A1** `approval_decision_inconsistent` kept as a guard; **A2** plan clearing applies to all `rejected`/`not_executable` graph results, no existing assertion needed a change for it; **A3** JSON-native defaults validated at `pin()`, not in the constructor. **Verified:** 108 new tests (`tests/test_approval_integrity.py`; 85 at the first commit, 23 added after an independent review), 23 mutation checks all killed, full suite 601 passed (493 → 601), `ruff` unchanged and `mypy` 3 errors fewer vs the base. Not merged — PR review pending. Limits (ADR-0003 §10.8): in-place runtime mutation, process restart (Q3), thread safety, private-dict writes | `[P§24]`, `[P§29.8]`, `[P§34]`, `[P§35]` | ADR-0003 §10, ADR-0009 §14, ADR-0010 §17 | accepted |

| D-033 | 2026-09-23 | Owner decisions (Tanvir) resolving `OPEN_QUESTIONS.md` Q7 and lifting `STATUS.md`'s "real LLM providers" hold, scoped exactly as stated: (1) **Anthropic is the first real provider adapter** (`cv_agent/llm/anthropic_provider.py`, `AnthropicProvider`), registered lazily in `cv_agent.llm.registry`. (2) **The gateway remains provider-agnostic** — `LLMProvider`/`LLMRequest`/`LLMResponse` (`cv_agent/llm/base.py`) unchanged; no provider name/SDK import outside `cv_agent/llm/anthropic_provider.py` (enforced by a structural test, ADR-0002 §7). (3) **Exactly one configured model** for the initial implementation: `claude-sonnet-4-5` (asked and confirmed explicitly — not invented). (4) **No automatic provider or model fallback** — `get_provider()` resolves the requested name/model or raises; nothing substitutes a different provider/model on failure. (5) **Credentials via environment, never config/TOML** — `AnthropicProvider` reads `ANTHROPIC_API_KEY` directly from the process environment at construction; `AgentConfig`/`LLMConfig` carry no secret fields. (6) **`FakeLLMProvider` stays the default and remains fully available** — `LLMConfig`'s default is unchanged (`provider="mock"`); every existing caller/test needs zero credentials. Explicitly NOT resolved by this decision (unchanged, still open): `OPEN_QUESTIONS.md` Q6 (approval-gate cost thresholds) and Q19 (cost-estimation mechanism) — no call/spend budget is invented or enforced; a multi-provider routing policy — moot with exactly one provider. See ADR-0002 for the full design | `[P§20]`, `[P§24]`, `[P§34]`, `[P§35]` | ADR-0002 | accepted |

| D-034 | 2026-09-24 | Correction to D-033/ADR-0002, found in PR #52 review (Tanvir) before merge: the configured Anthropic model is **`claude-sonnet-5`**, not `claude-sonnet-4-5`. Verified, not assumed: Anthropic's docs state the undated alias for any pre-4.6-generation model (Sonnet 4.5 is one) "resolves to the dated ID" — confirming `claude-sonnet-4-5` was never pinned, and `claude-sonnet-4-5-20250929` is the real dated snapshot (also present in the installed `anthropic` SDK's `ModelParam` type literal). Further investigation (fetched Anthropic's live model-overview docs) found Sonnet 4.5 is now a legacy model, superseded by Sonnet 5; for 4.6+-generation models "every... ID is a pinned snapshot, including the dateless IDs... the alias row repeats them" — `claude-sonnet-5` has no drift problem to begin with, unlike a dated Sonnet-4.5 snapshot on a family with no committed long-term retirement date. Owner chose `claude-sonnet-5` over dating the legacy family. No adapter/interface/test-shape change — only the literal model-ID string in `cv_agent/llm/anthropic_provider.py`'s docstring, `config/default.toml`'s example, `tests/test_llm_anthropic.py`, and ADR-0002 itself. D-033 is left unedited per this ledger's append-only convention; ADR-0002 §9 records the same correction with full reasoning | `[P§20]`, `[P§29.5]` | ADR-0002 §9 | accepted |

| D-035 | 2026-09-24 | ADR-0005 (Tool/MCP boundary) drafted — **Proposed, not accepted**, no implementation. Defines `cv_agent.tools` as a new, dependency-free leaf package: `ToolId`/`ToolSpec`/`ToolRequest`/`ToolResult`/`ToolError`, a `ToolInvoker` `Protocol` (shape-symmetric to, never substitutable for, ADR-0009's `ExecutionRuntime`), `ToolRegistry` (explicit registration only), and `ToolExecutor` (fail-safe verification/approval/pin branching mirroring `SkillExecutor.execute()`). Zero tools registered, zero invokers implemented, no MCP SDK/vendor selected. Explicitly does not resolve `OPEN_QUESTIONS.md` Q5's MCP-vs-CLI/skill half (left unresolved, file untouched), nor Q2/Q3/Q6/Q10/Q16/Q19/Q23. Two new, non-blocking open items raised in the ADR itself (§10), not added to `OPEN_QUESTIONS.md`: how MCP resources/prompts (distinct from tools) would map onto this boundary once an SDK is chosen; and that ADR-0001 (source of the `ToolId` concept) is itself still `Proposed`, not `Accepted` (D-005), so this ADR's `ToolId` has no accepted upstream identity to unify with yet. `docs/architecture/OVERVIEW.md`'s Tools/MCP row and `docs/roadmap/ROADMAP.md` Phase 2 status updated to reference the draft; no other documentation or code changed | `[P§22]`, `[P§15]`, `[P§19]`, `[P§21]`, `[P§23]`, `[P§34]` | ADR-0005 | proposed |

| D-036 | 2026-09-24 | ADR-0005 (Tool/MCP boundary) **accepted** by owner (Tanvir) following an architecture review that found zero blockers and five IMPORTANT findings, all resolved in a same-day revision before acceptance (see ADR-0005 §11 for the full list and resolution). Implemented per the accepted interface: `cv_agent/tools/` — `models.py` (`ToolId`, `ToolTransport`, `ToolResultStatus`, `ToolErrorCategory`, `ApprovalPolicy`, `ToolInputField`, `ToolRequiredFieldGroup`, `ToolSpec`, `ToolRequest`, `ToolError`, `ToolEvidence`, `ToolResult`, `ToolOutcome`), `invoker.py` (`ToolInvoker` protocol), `registry.py` (`ToolRegistry` with invoker-registration-generation tracking, `pin()`, `tool_pin_is_well_formed()`, `tool_pin_mismatch()`), `executor.py` (`ToolExecutor`, fail-closed branching mirroring `SkillExecutor.execute()` as amended by ADR-0009 §14). Zero tools registered, zero invokers implemented, no MCP SDK/vendor selected, nothing wired into `CVAgent`/`LangGraph`/`SkillExecutor`/existing execution bindings — the boundary only, exactly as ADR-0009 §1 held the line for skill execution until `trt-perf-analysis`. `cv_agent.tools` imports nothing from `cv_agent.execution`/`cv_agent.skills`/`cv_agent.graph`/`cv_agent.llm` (enforced by a structural test). 71 new tests (`tests/test_tools.py`), full suite 646 → 717 passing, zero regressions; `ruff check cv_agent tests` and `mypy cv_agent` both clean (44 source files). Two manual mutation checks (E1 pin-required rule; invoker-generation-mismatch comparison) each made the relevant test(s) fail, then reverted. `OPEN_QUESTIONS.md` unchanged; Q2/Q3/Q5/Q6/Q10/Q16/Q19/Q23 remain unresolved. Branch `feature/claude/adr-0005-tool-mcp-boundary`, issue #53. D-035 (the draft decision) is left unedited per this ledger's append-only convention — this row records the subsequent acceptance and implementation as a new decision, mirroring how D-034 corrected D-033 without editing it | `[P§22]`, `[P§15]`, `[P§19]`, `[P§21]`, `[P§23]`, `[P§34]` | ADR-0005 | accepted |

| D-037 | 2026-09-24 | ADR-0006 (Knowledge/Context boundary) drafted, self-reviewed, and implemented under the owner's explicit continuous-execution authorization ("if it is unblocked: create feature branch; implement... do not stop"), a compressed version of ADR-0005's separate draft→review→revise→accept cycle — the self-review is documented in the ADR's own §9 against the same five criteria (boundary correctness, safety/integrity, interface quality, governance, documentation consistency) an independent reviewer used for ADR-0005, and found no blocker. Selected over the two gaps that sit earlier in `OVERVIEW.md`'s declared build sequence (experiment-ledger persistence, a durable checkpointer) because both are blocked by still-open owner decisions (Q16, Q3 respectively) that this session is not authorized to make; selected over Phase 3/4 skill-binding/CLI-wiring gaps for the same reason (Q23). New leaf-ish package `cv_agent/knowledge/`: `Provenance`/`KnowledgeItem` (fail-closed at construction — no item can exist without valid provenance, directly implementing `docs/RESEARCH_POLICY.md`'s "a stored item missing provenance or date is deleted, not kept" even earlier than storage), a fixed `SourceClass → EvidenceWeight` mapping taken verbatim from that document's table (not a per-item override, so a `professional_post`/LinkedIn source can never claim `"high"` weight — the LinkedIn rule `[P§17]` enforced by construction, not convention), `KnowledgeStore` `Protocol` + `InMemoryKnowledgeStore` (deliberately no durable backend — mirrors ADR-0005's "boundary only" scoping rather than inventing a Q16-equivalent decision for this layer), and deterministic `assemble_context()` (exact tag/source-class/staleness filtering, a fully explicit total-order tie-break — evidence weight desc, date_accessed desc, item_id asc — never embeddings or a ranking model). `cv_agent.knowledge` imports nothing from `cv_agent.execution`/`skills`/`graph`/`tools`/`llm` (structural test, mirrors ADR-0005 §7). 55 new tests (`tests/test_knowledge.py`), full suite 717 → 772 passing, zero regressions; `ruff check cv_agent tests` and `mypy cv_agent` both clean (48 source files). `OPEN_QUESTIONS.md` unchanged; Q2/Q3/Q5/Q6/Q10/Q16/Q19/Q23 remain unresolved. Not self-merged (unlike PR #52/#54) — no fresh explicit merge instruction was given for this PR; stops at the owner-review boundary per CLAUDE.md §6.3. Branch `feature/claude/adr-0006-knowledge-context-boundary`, issue #55 | `[P§16]`, `[P§17]`, `[P§18]`, `[P§19]`, `[P§29.3]`, `[P§34]`, `[P§35]` | ADR-0006 | accepted |

| D-038 | 2026-09-24 | Owner decision (Tanvir) resolving `OPEN_QUESTIONS.md` **Q16**: the experiment ledger's persistence backend is **SQLite**, mirroring `cv_agent/memory/`'s established pattern (ADR-0004, Q8/D-017). Implemented as new package `cv_agent/experiments/` (ADR-0011, issue #57) — `models.py` (`ExperimentRecord`, plus `HardwareInfo`/`LatencyMeasurement`/`MemoryFootprint`), `store.py` (`ExperimentLedgerError`, `ExperimentLedger` `Protocol`, `default_db_path()`, `open_ledger()` factory), `sqlite_store.py` (`SqliteExperimentLedger`, the only new module permitted to reference `sqlite3`, enforced by the pre-existing repo-wide filename-based structural test in `tests/test_memory.py`). Every field name is taken verbatim from `docs/state/EXPERIMENTS.md`'s `[P§25]` schema table — none renamed or dropped. Three schema rows whose own prose names two values under one field (`hardware`: training **and** target; `latency`: end-to-end **and** inference-only; `memory`: VRAM + RAM) are represented as small nested frozen dataclasses under the same top-level field name, rather than split into new field names (a silent rename) or collapsed to one value (a silent drop). Mutability follows `EXPERIMENTS.md` rule 1 ("never edit a row after the run completes") exactly: `record_experiment()` upserts while `status` is `proposed`/`running`, and raises `ExperimentLedgerError` once the stored row is `completed`/`failed`/`cancelled` — a rule with no direct precedent in `cv_agent/memory/` (neither pure immutable-append nor unrestricted upsert), required by the rule's own wording. A separate `experiments.sqlite` file (same gitignored `.cv_agent/` directory as `memory.sqlite`), not a table added to project memory's database — `cv_agent/memory/` has no established mechanism for a second package to extend its schema. Where the spec is silent, only minimal deterministic integrity constraints were added and documented per field (exp_id format `EXP-YYYYMMDD-NN` taken from the schema's own stated format; terminal status requires `completed_at`; `completed_at` cannot precede `created_at`; metrics/numeric fields finite and non-negative where physically required; `status` CHECK constraint at the DB level too); no dataset/model registry cross-validation (none exists — Phase 5 not started, Q10 open). One SQL-keyword collision handled: the domain field `commit` is stored in column `commit_sha`. 90 new tests (`tests/test_experiments.py`), full suite 772 → 862 passing, zero regressions; `ruff check cv_agent tests` and `mypy cv_agent` both clean (52 source files). One false-positive caught in the pre-existing sqlite3-confinement test (a literal substring match on `store.py`'s own docstring prose) and fixed by rewording the docstring, not by editing that test. Not wired into `CVAgent`/the CLI. Q3 and Q23 remain unresolved and are not implied by this decision. Branch `feature/claude/q16-sqlite-experiment-ledger`, issue #57 | `[P§25]`, `[P§29.2]`, `[P§29.5]`, `[P§34]`, `[P§35]` | ADR-0011 | accepted |

| D-039 | 2026-09-24 | Owner-authorized ROADMAP Phase 5a implemented: the storage-agnostic **Dataset Core**, `cv_agent/datasets/` (ADR-0012, issue #59), after PR #58 (Q16) was squash-merged as `44539c3` (owner-authorized merge; CI green on the merged commit). **`DatasetManifest` is the dataset version**, identified by `(dataset_id, version)` — `docs/DATA.md`'s own terms; frozen, tuple-based, validated at construction, and it refuses to exist unless its split passes all five leakage checks (temporal, camera, subject, annotation-round, supplied-hash near-duplicate); the report is computed by the constructor, recorded in the manifest, not caller-suppliable. A change is a new version with `lineage`, never an update. `DatasetStore` is a `Protocol` (`put_manifest`/`get_manifest`/`list_versions`: never overwrite, lineage parent must exist, failed put changes nothing) with an `InMemoryDatasetStore` reference; no backend, no factory, no registry. Where `docs/DATA.md` is silent (item attributes; which goal applies; annotation round→guideline; Hamming tolerance; unverifiable input) the interpretations are named in ADR-0012 §3 as `[P§35]` gaps, all fail-closed. **Q10 is not decided** (no DVC/LFS/object store, no persistence format), no hash is computed from pixels, no dependency added (stdlib only, structurally enforced), nothing wired into `CVAgent`/ledger; Q2/Q3/Q4/Q5/Q6/Q19/Q23 untouched. 88 new tests incl. 11 manual mutation checks all killed; `ruff`/`mypy cv_agent` clean | `[P§26]`, `[P§25]`, `[P§29.5]`, `[P§34]`, `[P§35]` | ADR-0012 | accepted |

## Reversals

Record here when a decision is overturned. Never delete the original row — mark it
`superseded` and add the new row with a pointer.

| # | Date | Reverses | Why | New ADR |
|---|---|---|---|---|
| — | — | — | — | — |
