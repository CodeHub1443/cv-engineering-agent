# DECISIONS

> **Append-only, one line per decision.** The ledger of what is settled, so a fresh
> session can absorb the project's commitments in thirty seconds without reading every
> ADR. Any *architectural* decision listed here must have an ADR; if it does not, write
> the ADR before committing.

| # | Date | Decision | Canon | ADR | Status |
|---|---|---|---|---|---|
| D-001 | 2026-08-30 | `docs/PROJECT.md` is frozen canon; all normative statements cite `[P§n]`; changes to intent happen only via ADR | `[P§35]` | — | accepted |
| D-002 | 2026-08-30 | Rolling state split into *state* (`STATUS.md`, rewritten) and *history* (`JOURNAL.md`, append-only) | `[P§25]` | — | accepted |
| D-003 | 2026-08-30 | No implementation code without an issue; no architectural code without an ADR | `[P§34]` | — | accepted |
| D-004 | 2026-08-30 | Design order fixed: registry → gateway → orchestration state → memory → tools → knowledge → skills → stages → execution | `[P§33]`, `[P§34]` | — | accepted |
| D-005 | 2026-08-30 | Capability / skill / tool / agent kept as four distinct typed registries | `[P§23]` | ADR-0001 | **proposed** |
| D-006 | 2026-08-30 | Two independent canons, not one chain: `docs/PROJECT.md` (knowledge) and `CLAUDE.md` (instructions, mirrored by `AGENTS.md`). Raw pre-canon draft moved from repo root to `docs/archive/PROJECT_SOURCE_DRAFT.md`, marked superseded and non-authoritative | `[P§35]` | — | accepted |
| D-007 | 2026-09-01 | Retired `spec/00,01,02,07,09` (fully duplicated `docs/PROJECT.md` or conflicted with ADR-governed `docs/architecture/OVERVIEW.md` / `docs/APPROVALS.md` / `docs/state/EXPERIMENTS.md`); reduced `spec/03,05` to technical elaborations citing the canon; merged unique gated actions from `spec/07` into `docs/APPROVALS.md`; extended `docs/state/EXPERIMENTS.md`'s schema with `spec/09`'s useful fields (`status`, `parent_exp_id`, `approval_ref`, timestamps) | `[P§35]` | — | accepted |
| D-008 | 2026-09-01 | Git workflow: single trunk `main` + short-lived `feature/<owner>/<work>` branches, PR reviewed and merged by the project owner. Two-trunk `main`/`dev-munna` model with a two-stage PM approval gate is retired — `dev-munna` is not a mandatory or permanent branch. `CLAUDE.md`, `AGENTS.md`, `docs/development/GITHUB_FLOW_V1.md`, `docs/development/GITHUB_DEVELOPMENT_MODEL.md` all updated to describe the same model | — | — | accepted |
| D-009 | 2026-09-01 | Capability registry semantics: DECLARED CAPABILITY != EXECUTABLE CAPABILITY. Introduced `status: "planned"` (specified, no executable binding) distinct from `"available"` (specified AND a verified executable binding exists). All 20 capabilities in `spec/capability_registry.json` corrected from `"available"`/`"partial"` to `"planned"` — none has an executable binding. `check_item()` no longer hardcodes `available: True`; reports `executable: False` with a reason for every registry item. `select()`/`is_available` now require `status == "available"` strictly | `[P§23]` | ADR-0001 (§8a) | accepted |
| D-010 | 2026-09-08 | Step 2 foundation: `cv_agent/skills/` adds a `Skill` domain model (independent from `RegistryItem`), a `SkillSource` protocol, `LocalSkillSource` (scans `~/.claude/skills` + `~/.agents/skills`, overridable via `CV_AGENT_SKILL_PATHS`), `SkillInventory`, and `TaskResolver` — deterministic keyword-overlap resolution only, no LLM. Three-state availability model made explicit: DECLARED (in `spec/capability_registry.json`) / DISCOVERED (found on disk by a `SkillSource`) / EXECUTABLE (a verified invocation binding — none exists, so every `Skill.executable` is `False`). New CLI: `skills`, `capabilities`, `resolve "<task>"` | `[P§23]`, `[P§15]` | ADR-0007 | accepted |
| D-011 | 2026-09-15 | Step 4 foundation (Step 3 — skill execution — was requested but never implemented; skipped, not silently assumed done): `cv_agent/requirements/` adds `RequirementsAnalyzer`, deterministic keyword-rule-based known/unknown/assumed field extraction (a field is `"assumed"` only via an explicit caller-supplied assumption — the analyzer never self-promotes), CV task-component hypotheses with stated rationale (`FIELD_DETECTORS`/`TASK_HYPOTHESIS_RULES` as flat, appendable data, not a decision tree), capability links produced by calling the existing `TaskResolver` (not bypassed, not duplicated), and targeted clarification questions. Optional `LLMProvider` (mock only) used solely for a `narrative_summary` field, never as a source of any structured fact. New CLI: `analyze "<request>"`. `CVAgent.analyze_requirements()`. Does not execute anything | `[P§5]`, `[P§6]`, `[P§23]` | ADR-0008 | accepted |
| D-012 | 2026-09-15 | Step 3 (redone properly, not skipped this time): `cv_agent/execution/` adds the skill execution boundary — `SkillExecutionRequest`/`Result`/`Status` (`not_executable\|started\|completed\|failed\|rejected`), `ExecutionBinding` (skill_id -> runtime_id, `approval_policy`, `verified: bool`), a provider-agnostic `ExecutionRuntime` protocol, a deterministic `ExecutionBindingRegistry`, and `SkillExecutor`. Inspected all 84 real installed skills first: every `SKILL.md` is prose for an LLM agent to read and act on with its own tools (28 declare Claude-Code-native `allowed-tools:` frontmatter), not a program with a verifiable invocation contract — so **zero bindings are registered anywhere in this codebase**; building a fake adapter that treated "reading SKILL.md" as execution was explicitly rejected as silent invention. `CVAgent.execute()`/`.can_execute()`/`.execution_bindings`; new CLI `executions`, reporting `Executable: 0/84` against the real environment, honestly | `[P§15]`, `[P§21]`, `[P§22]`, `[P§23]`, `[P§24]`, `[P§34]` | ADR-0009 | accepted |
| D-013 | 2026-09-15 | Orchestration state + human-approval interrupts (identified by a repository alignment review as the actual critical-path blocker, not Research/RAG): `cv_agent/graph/workflow.py` adds a second, separate compiled LangGraph graph using the real dynamic-interrupt API (`langgraph.types.interrupt()` + `Command(resume=...)`, confirmed empirically against the installed langgraph 1.2.11 — no custom sleep/polling) for requirements-clarification (loops back to `analyze_requirements` exactly once after a human answers, never interrupts twice) and approval-gated execution (`approval_gate` node reads a binding's `approval_policy` via `SkillExecutor.get_binding()`, interrupts if `approval_required`, and the resume value — not any other code path — becomes `approval_decision`; the existing `SkillExecutor.execute()` from ADR-0009 still does the actual allow/reject enforcement, so the two layers can't disagree). `AgentState` extended with `requirements_analysis`/`clarification_answers`/`pending_execution`/`approval_decision`/`execution_result`, stored as plain dicts (`dataclasses.asdict()`), not reasoning-layer dataclass instances — LangGraph's checkpoint serializer already warns unregistered classes will be rejected in a future version. Kept as a *second* graph, not inserted into the existing `build_graph()`/`run()` stub — existing tests hard-assert that topology's exact current behavior. Partially resolved `OPEN_QUESTIONS.md` Q1/Q2 (don't block this ADR after all) and Q3 (transport is the interrupt; durable/restart-survivable resume is a checkpointer swap, deferred — `MemorySaver` does not survive process restart, confirmed). `CVAgent.start_workflow()`/`.resume_workflow()`/`.get_workflow_state()`; new CLI `workflow` | `[P§5]`, `[P§10]`, `[P§21]`, `[P§24]`, `[P§29.8]`, `[P§34]` | ADR-0003 | accepted |
| D-014 | 2026-09-16 | First real execution binding (ADR-0009 §8 revisit trigger fired, for exactly one skill): `cv_agent/execution/runtimes/trt_perf_analysis.py` adds `TrtPerfAnalysisRuntime` (an `ExecutionRuntime` implementing skill chosen by inspecting the real installed `trt-perf-analysis` skill directory, not assumed from ADR-0009's own prior mention of it), `build_binding()`, and an explicit, opt-in `register(registry)` — `CVAgent.__init__` still constructs an empty registry; nothing calls `register()` automatically, so `python -m cv_agent executions` against a fresh `CVAgent` still reports `0/84` unless a caller has explicitly opted in. The runtime derives the script's location from the real, discovered `Skill.location` (never a hard-coded path) and runs `scripts/analyze_trt_perf.py` as a real subprocess with this process's own interpreter — verified empirically, not assumed: exit `0` + one JSON object on stdout on success (including a per-backend-failed-validation case, which the script itself treats as a valid outcome, not a crash), exit `2` + a one-line stderr message when no input files exist at all. `approval_policy="allowed"` (read-only, local, deterministic, Python-stdlib-only — no approval gate needed per `CLAUDE.md` §3 rule 10), `verified=True` for this one binding only. No other skill's `Skill.executable` changes. See ADR-0009 §9 | `[P§15]`, `[P§21]`, `[P§22]`, `[P§23]`, `[P§29.9]`, `[P§34]` | ADR-0009 | accepted |
| D-015 | 2026-09-15 | `OPEN_QUESTIONS.md` Q1 resolved by owner decision: V1 uses **one CV project per repository/workspace** — the workspace/repo IS the project boundary, no `project_id`/multi-project/multi-tenant abstraction in V1, no broader multi-repository workspace abstraction. Project Understanding is persistent current state with recoverable, immutable revision history; experiments stay immutable append-only records (unchanged from `EXPERIMENTS.md`); session identity (`AgentState.session_id`) stays distinct from and subordinate to project identity — every session belongs to the one implicit project. ADR-0004 records this and defines the memory subsystem's boundary: `cv_agent/memory/` — `ProjectUnderstandingRevision`, `SessionRecord`, and a `ProjectMemoryStore` `Protocol`, **interface only**, zero implementation, nothing wired into `CVAgent`/the graph/CLI yet. Storage backend deliberately left undecided (`OPEN_QUESTIONS.md` Q8 remains open and is the actual implementation blocker) | `[P§21]`, `[P§25]`, `[P§26]`, `[P§30]`, `[P§33]`, `[P§34]`, `[P§35]` | ADR-0004 | accepted |
| D-016 | 2026-09-15 | `OPEN_QUESTIONS.md` Q15 (new, raised and resolved same day — an ADR-0004 audit found the ADR named a storage backend as open, Q8, but never addressed Git-tracking or data sensitivity) resolved by owner decision: Project Understanding is **potentially sensitive project data**, governed by `docs/APPROVALS.md`'s data/privacy rule; it must be **durable across process restarts** but must **NOT** be automatically stored in Git-tracked repository files — V1 persistent storage is **local/project-scoped, gitignored by default**. Durability does not imply Git tracking. Does not change `EXPERIMENTS.md`'s contract. Does not grant any new external-LLM transmission permission — existing approval/privacy rules still govern that regardless of persistence. `OPEN_QUESTIONS.md` Q8 (storage *technology* — files/SQLite/service) stays open and unresolved, now narrowed to exclude "in-repo/tracked files" as an option for project memory specifically. ADR-0004 updated to record this (§1, §2, §6, §9) — no code, no backend chosen, no `cv_agent/memory/` implementation | `[P§24]`, `[P§25]` | ADR-0004 | accepted |
| D-017 | 2026-09-15 | `OPEN_QUESTIONS.md` Q8 resolved by owner decision, for **project memory only**: **SQLite**, local and project-scoped, living in the project's gitignored persistent-state area (per D-016 — not Git-tracked), must survive process restarts. SQLite is used strictly **behind** the `ProjectMemoryStore` `Protocol` (ADR-0004 §5) — no module outside `cv_agent/memory/` may depend on a SQLite-specific type, so a future backend can replace it without touching callers. No external database/service required for V1. Project Understanding revisions stay immutable; `SessionRecord`s keep their defined mutable-index lifecycle (ADR-0004 §3). `docs/state/EXPERIMENTS.md` is explicitly **not** moved into SQLite and its contract is unchanged — its own backend question is split off as new `OPEN_QUESTIONS.md` Q16, still open, non-blocking for ADR-0004. This resolves Q8 as ADR-0004's last remaining implementation blocker; ADR-0004 is now **ready for implementation** (no code written yet — `cv_agent/memory/` remains unimplemented) | `[P§21]`, `[P§25]`, `[P§29.5]`, `[P§34]` | ADR-0004 | accepted |
| D-018 | 2026-09-15 | ADR-0004 implemented: `cv_agent/memory/` — `models.py` (`ProjectUnderstandingRevision`, `SessionRecord`, backend-neutral), `store.py` (`ProjectMemoryStore` `Protocol`, `ProjectMemoryError`, `default_db_path()` — zero `sqlite3` references), `sqlite_store.py` (`SqliteProjectMemoryStore` — the only module in the codebase importing `sqlite3`, verified by a structural test). Two tables (`understanding_revisions` append-only with autoincrement ordering; `sessions` upserted by `session_id`), idempotent schema init, every write transactional, every failure raised as `ProjectMemoryError`, never swallowed. `default_db_path()` resolves from `Path.cwd()` (or an explicit `workspace_root`), deliberately not from `__file__`'s install location, so an installed `cv-agent` does not silently share one database across unrelated projects — the concrete enforcement of the Q1 "workspace IS the project" decision at the filesystem level. `.gitignore` gained `/.cv_agent/`. 28 new tests (`tests/test_memory.py`): init, empty-store, immutable/ordered revision history, duplicate-id rejection, session upsert, persistence across close/reopen (including 3 restart cycles), closed-connection error behavior, and the `sqlite3`-confinement structural test. Deliberately **not** wired into `CVAgent`/the workflow graph/CLI (ADR-0004 §6/§9's own stated scope) — that remains a separate, later PR. Full suite 195 → 223 passing, zero regressions. Branch `feature/claude/project-memory`, not committed to `main` | `[P§21]`, `[P§25]`, `[P§30]`, `[P§34]` | ADR-0004 | accepted |
| D-019 | 2026-09-15 | Workspace-root resolution contract clarified by owner decision, following a post-implementation audit that found `default_db_path()`'s `Path.cwd()` fallback had no actual guarantee behind it (no CLI flag, no `CVAgent` parameter, no other mechanism anywhere ties process execution to a directory; the installed `cv-agent` console script is invocable from anywhere — silent failure modes: wrong-directory invocation, subdirectory fragmentation, or two unrelated projects sharing one `.cv_agent/` by coincidence). Resolved: **the calling application is responsible for resolving `workspace_root`**, never `ProjectMemoryStore`/`default_db_path()` itself; `Path.cwd()` remains only as a documented convenience default for direct/standalone use, explicitly not the application contract; the not-yet-built `CVAgent`/CLI integration (ADR-0004 §9) must resolve and pass `workspace_root` explicitly when it wires a store in; automatic repository-root discovery (`.git`-walking) is explicitly out of scope for V1, named as a revisit trigger instead. Documentation-only — ADR-0004 §1 (new item 13), §2, §5, §8, §9 updated; no code changed, `cv_agent/memory/` behavior is unchanged (it already accepted an explicit `workspace_root` override) | `[P§21]`, `[P§34]` | ADR-0004 | accepted |
| D-020 | 2026-09-15 | Project Memory (ADR-0004) wired into the runtime, minimally: `AgentConfig.workspace_root: Path \| None = None` (new field, not TOML-sourced — a per-invocation fact, not a static setting); `cv_agent.memory.store.open_store()` (new factory, mirrors `cv_agent.llm.registry.get_provider()` — callers ask for "the store," never import `SqliteProjectMemoryStore` directly, keeping SQLite confined even at this package's own public entry point); `CVAgent.memory` (public, **lazily** constructed on first access — `health_check()`/`resolve()`/`analyze_requirements()`/`execute()` never touch disk for this, only `start_workflow()`/`resume_workflow()` do); a `SessionRecord` written before the graph runs (status "running") and updated after (status/`ended_at` from the resulting `AgentState`, restart-safe — re-running `start_workflow()` for an existing `session_id` preserves its original `started_at`). Revision-trigger policy (ADR-0004 §9, previously deferred to this PR): append a new `ProjectUnderstandingRevision` iff a run's `requirements_analysis` differs, by JSON-normalized structural equality **excluding `narrative_summary`/`llm_provider`**, from `get_current_understanding()` — not semantic diffing, a fixed comparison. Two things were found and fixed only by writing the integration tests, not anticipated in ADR-0004: (1) `AgentState["requirements_analysis"]`'s container types are not stable — tuples on a fresh `.invoke()`, lists once round-tripped through a LangGraph checkpoint — a bare `==` against this store's own JSON-stored value reported "different" for identical data; (2) even the mock LLM provider re-words `narrative_summary` on every call (a response counter), so comparing prose meant the dedup *never* fired, including for a plain restart of the same task — excluded via a fixed field list grounded in ADR-0008's own "prose, never a source of fact" classification. Also fixed: `tests/test_workflow.py`'s `TestCVAgentWorkflowWiring` and `tests/test_cli.py`'s CLI-subprocess `_run()` helper had been constructing `CVAgent()`/running the CLI with no explicit `workspace_root`/`cwd` — since default resolution now falls back to `Path.cwd()`, both were silently creating a real `.cv_agent/memory.sqlite` inside this repository's own working directory on every test run, undetected until this integration made memory-writes observable; both pinned to their existing `tmp_path` fixture. `cv_agent/graph/workflow.py` itself is untouched — no memory call inside any graph node; persistence happens in `CVAgent` wrapping the graph invocation, keeping ADR-0003's checkpoint/interrupt mechanics and this ADR's durable store two separate concerns, per ADR-0004 §2. 24 new tests (21 in new `tests/test_memory_integration.py` + 3 `open_store()` tests in `tests/test_memory.py`). Full suite 223 → 247 passing, zero regressions. Known limitation, not fixed here (`ProjectMemoryStore`'s interface has no cross-write transaction primitive): the session-record write and the revision write inside one sync call are two independent atomic operations, not one — a failure between them is a narrow, acknowledged gap, not silently ignored. Branch `feature/claude/project-memory`, not committed to `main` | `[P§21]`, `[P§25]`, `[P§30]`, `[P§34]` | ADR-0004 | accepted |
| D-021 | 2026-09-16 | Closed the requirements -> resolution -> execution feedback loop at the application layer (an architecture audit found this, not another execution binding/RAG/research/real LLM/persistent checkpointing, was the actual highest-value gap: the one real binding from D-014 was genuinely invocable only from Python/tests, never from the CLI, and `Skill.executable`/`SkillMatch.executable` were hardcoded `False` everywhere, disconnected from the real `ExecutionBindingRegistry`). Two amendments, no new ADR: **(1) ADR-0007 §9** — `SkillInventory` gains an optional `is_executable: Callable[[str], bool] \| None` predicate (never a concrete `cv_agent.execution` import — `cv_agent.skills` stays execution-agnostic per this ADR's own §2 boundary); `CVAgent.__init__` wires `self._executor.can_execute` in; `TaskResolver` now copies `skill.executable` instead of hardcoding `False`. No predicate given (every pre-existing direct `SkillInventory(...)` construction) reproduces the exact prior behavior. **(2) ADR-0009 §10** — new `python -m cv_agent execute <skill_id>` CLI command, real `CVAgent -> SkillExecutor -> ExecutionBindingRegistry -> ExecutionRuntime` path, never bypassed/duplicated; only `trt-perf-analysis` is accepted (a single constant check, not a dispatch table); this command is the one explicit, controlled caller that registers that binding into its own short-lived `CVAgent`; `--path`/`--input KEY=VALUE`/`--model-name` build the real `SkillExecutionRequest`; `--approve` or a live "y"/"yes" prompt answer is required for a hypothetical future `approval_required` binding — never auto-approved (`_confirm_approval()`, unit-tested against a fake binding since `trt-perf-analysis`'s own policy is `"allowed"`). `skills`/`resolve`/`capabilities`/`executions` are untouched and still report `executable=False`/`0/84` by default — no auto-registration anywhere outside this one command. 32 new tests (10 in `tests/test_skills.py`/`tests/test_agent.py` for the resolution wiring, 17 in new `tests/test_cli_execute.py` for the CLI, plus a genuine, unfaked end-to-end CLI smoke test against the real installed skill). Full suite 278 → 305 passing, zero regressions. Also fixed `docs/state/STATUS.md`'s stale "PR #27 still open" statement (merged as `2cf3709`). Branch `feature/claude/execution-feedback-loop`, not committed to `main` | `[P§15]`, `[P§19]`, `[P§21]`, `[P§22]`, `[P§23]`, `[P§24]`, `[P§34]` | ADR-0007, ADR-0009 | accepted |
| D-022 | 2026-09-16 | Exposed matched skill information, including live executable status, from `RequirementsAnalysis` (an architecture audit of merged `main` @ `7c284d2` identified this as the single highest-priority next step: `RequirementsAnalyzer._link_capabilities()` was already calling `TaskResolver.resolve()` once per candidate task component and reading `result.matched_capabilities`, but silently discarded `result.matched_skills` — including the truthful `executable` flag D-021 had just wired up — on the same line, so a user running `analyze` had no way to learn which matched capability actually had a right-now-executable skill behind it). **ADR-0008 §9 amendment:** `RequirementsAnalysis` gains a new top-level `skill_links: tuple[SkillLink, ...]` field (`SkillLink`: `task_component`, `skill_id`, `declared`, `matched_terms`, `executable`) — deliberately NOT nested inside `CapabilityLink`, since a resolver's `SkillMatch` is not attributed to one specific capability within a single `resolve()` call (a skill can be matched purely by keyword overlap, independent of any capability's declared `relevant_skills`, or declared by more than one matched capability at once); `task_component` is the same join key `CapabilityLink` already uses. `_link_capabilities()` now returns `(capability_links, skill_links)` from the SAME `resolve()` call already in scope — no second resolution pass (verified by an explicit call-count test). `executable` is copied verbatim from `SkillMatch.executable`, itself copied from the calling `CVAgent`'s live execution registry at analysis time (ADR-0007 §9) — never a permanent property of the skill, never independently computed. No `selected_skill` field, no ranking, no confidence-of-selection — `RequirementsAnalyzer` still does not decide which skill should execute (ADR-0008 §2's boundary, unchanged, now simply made visible rather than moved). `python -m cv_agent analyze` gained a "Matched skills" section, formatted consistently with `resolve`'s own output; `analyze` still constructs a fresh, unregistered `CVAgent` and never registers a binding itself, so a bare CLI invocation still always reports `executable=False`. 15 new tests (9 in `tests/test_requirements.py::TestSkillLinks` — empty/one-skill/multiple-skills/multiple-capabilities/task-component-scoping/no-second-call using a duck-typed fake resolver for precise control, plus executable True/False/unverified-binding against a real fixture skill named `trt-perf-analysis`; 4 in `tests/test_agent.py` — `CVAgent.analyze_requirements()` end-to-end including one genuine, unfaked test against the real installed `trt-perf-analysis` skill and its real shipped binding; 2 in `tests/test_cli.py`). Full suite 305 → 320 passing, zero regressions. Also fixed, per this task's explicit instruction: `docs/roadmap/ROADMAP.md` Phase 3's stale "zero bindings are registered... no `ExecutionRuntime` implementation exists for any real skill yet" and Phase 4's stale "no memory subsystem exists" status statements, both predating D-020/D-021. Branch `feature/claude/requirements-skill-links`, not committed to `main` | `[P§5]`, `[P§6]`, `[P§23]`, `[P§34]` | ADR-0008 | accepted |

## Reversals

Record here when a decision is overturned. Never delete the original row — mark it
`superseded` and add the new row with a pointer.

| # | Date | Reverses | Why | New ADR |
|---|---|---|---|---|
| — | — | — | — | — |
